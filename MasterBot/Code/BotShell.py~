import socket
import time
import commanders
import Runcode
import BotTalking
import os

run = 1
Connected = 1
os.chdir('..')
print dir(os)
os.system('dir')
while Connected:
    Socker = socket.socket()
    Storg = Runcode.Datas(open('Data.txt','r'))
    Runcode.init(Socker,Storg)
    Connected,run = 1,1;print "Connected!"
    while run >=0:
        Storg = Runcode.Datas(open('Data.txt','r'))
        run = 1; print "Booted"
        while run > 0:
            Connected,run = Runcode.running(Socker,Storg)
        reload(commandant)
        reload(Runcode)
    Socker.close()
    reload(BotTalking)
