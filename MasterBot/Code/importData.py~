import os

def initData():
    if not os.path.iddir('./UserData/'):
        os.path.mkdir('./UserData/')
    if not os.path.isfile('./UserData/Data.txt'):
        os.system('cp ./NewData/*.* ./UserData/\n')
